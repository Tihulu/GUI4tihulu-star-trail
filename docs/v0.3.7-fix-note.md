# v0.3.7 launch-state fixes

- Serialize the visible Group / Trail / Timelapse hardware policy into Rust before each job launch, eliminating the GPU-to-Start race with an older Auto policy.
- Keep Photo Workspace output on the same Process output-path state. The Workspace output row mirrors Process immediately and uses the same picker/open actions, so the two views cannot drift apart.
